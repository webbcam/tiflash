{
    "paths": {
        "repo": "{{ env.paths.repo }}",
        "workspace": "{{ env.paths.workspace | default(env.paths.repo + '/tests/.workspace')}}",
        "tmp": "{{ env.paths.tmp | default(env.paths.repo + '/tests/.tmp')}}",
        "resources": "{{ env.paths.resources | default(env.paths.repo + '/tests/resources')}}",
        "ccxml": "{{ env.paths.ccxml | default(env.paths.home + '/ti/CCSTargetConfigurations')}}"
    }
}
